아래 문서에서 정보를 추출합니다. 먼저 각 필드를 단계별로 분석한 뒤, 최종 JSON을 출력하세요.

추출 대상 필드 (7개):
기관명 / 문서번호 / 작성일 / 제목 / 핵심내용 / 담당자 / 처리기한

문서:
{document}

---

단계별 분석:

1. 기관명: 문서에서 발신 기관 또는 담당 부서를 찾는다.
   → 찾은 값:

2. 문서번호: 문서 고유 번호를 찾는다.
   → 찾은 값:

3. 작성일: 작성일을 찾아 YYYY-MM-DD 형식으로 변환한다.
   → 찾은 값:

4. 제목: 문서의 제목을 찾는다.
   → 찾은 값:

5. 핵심내용: 문서 전체를 읽고 핵심 내용을 1~3문장으로 요약한다.
   → 찾은 값:

6. 담당자: 담당자 이름과 직책을 찾는다.
   → 찾은 값:

7. 처리기한: 처리 또는 제출 기한을 찾아 YYYY-MM-DD 형식으로 변환한다.
   → 찾은 값:

---

위 분석을 바탕으로 최종 JSON (JSON만, 다른 텍스트 없이):
